<Window x:Class="NyaWatch.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NyaWatch" 
        Icon="pack://application:,,,/Resources/icon_32x32.ico"
        Height="600" 
        Width="800">
    <StackPanel Orientation="Vertical">
        <!-- buttons -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Margin="10">
                <StackPanel.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Width" Value="40" />
                        <Setter Property="Margin" Value="10 0 5 0" />
                    </Style>
                </StackPanel.Resources>
                
                <Button ToolTip="Plan to watch">
                    <TextBlock FontFamily="./Resources/Fonts/#FontAwesome" Text="&#xf050;" />
                </Button>
                <Button ToolTip="Watching">
                    <TextBlock FontFamily="./Resources/Fonts/#FontAwesome" Text="&#xf04b;" />
                </Button>
                <Button ToolTip="Completed">
                    <TextBlock FontFamily="./Resources/Fonts/#FontAwesome" Text="&#xf04d;" />
                </Button>
                <Button ToolTip="On hold">
                    <TextBlock FontFamily="./Resources/Fonts/#FontAwesome" Text="&#xf04c;" />
                </Button>
                <Button ToolTip="Dropped">
                    <TextBlock FontFamily="./Resources/Fonts/#FontAwesome" Text="&#xf052;" />
                </Button>
            </StackPanel>

            <TextBox Grid.Column="1" Margin="5 0 10 0" Height="25">Search anime</TextBox>
        </Grid>
        
        <!-- animes -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Image Height="300" Width="200"></Image>
            
            <ListView Grid.Column="1" ItemsSource="{Binding Animes}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Gray" BorderThickness="1" Margin="5" Padding="5" MinWidth="550">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                            
                                <Border BorderBrush="Gray" BorderThickness="1">
                                    <Image Height="70" Width="40"></Image>
                                </Border>
                            
                                <StackPanel Grid.Column="1" Orientation="Vertical" MinWidth="300">
                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="{Binding Type}" 
                                               Foreground="{Binding TypeColor}" 
                                               MinWidth="50" />
                                    
                                        <Label Content="{Binding Title}" />    
                                    </StackPanel>
                                
                                    <Label Content="{Binding TorrentsMessage}" 
                                           Foreground="{Binding TorrentsColor}" Margin="20 0 0 0" />
                                </StackPanel>

                                <StackPanel Grid.Column="2" Orientation="Vertical">
                                    <Label Content="{Binding Status}" Foreground="{Binding StatusColor}" />
                                
                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="{Binding Watched}" />
                                        <Label>of</Label>
                                        <Label Content="{Binding Episodes}" />
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        
        <!-- torrents search -->
        <TextBox>Search torrents</TextBox>
        
        <!-- torrents -->
        <ListView></ListView>
    </StackPanel>
</Window>
