<Window x:Class="NyaWatch.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="600" Width="800">
    <StackPanel Orientation="Vertical">
        <!-- buttons -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <StackPanel.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Width" Value="100" />
                    </Style>
                </StackPanel.Resources>
                <Button>Plan to watch</Button>
                <Button>Watching</Button>
                <Button>Completed</Button>
                <Button>On hold</Button>
                <Button>Dropped</Button>
            </StackPanel>

            <TextBox Grid.Column="1">Search anime</TextBox>
        </Grid>
        
        <!-- animes -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Image Height="300" Width="200"></Image>
            
            <ListView Grid.Column="1" ItemsSource="{Binding Animes}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            
                            <Image Height="70" Width="40"></Image>
                            
                            <StackPanel Grid.Column="1" Orientation="Vertical" MinWidth="300">
                                <StackPanel Orientation="Horizontal">
                                    <Label Content="{Binding Type}" Foreground="{Binding TypeColor}" />
                                    <Label Content="{Binding Title}" />    
                                </StackPanel>
                                
                                <Label Content="{Binding TorrentsMessage}" Foreground="{Binding TorrentsColor}" />
                            </StackPanel>

                            <StackPanel Grid.Column="2" Orientation="Vertical">
                                <Label Content="{Binding Status}" Foreground="{Binding StatusColor}" />
                                
                                <StackPanel Orientation="Horizontal">
                                    <Label Content="{Binding Watched}" />
                                    <Label>of</Label>
                                    <Label Content="{Binding Episodes}" />
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        
        <!-- torrents search -->
        <TextBox>Search torrents</TextBox>
        
        <!-- torrents -->
        <ListView></ListView>
    </StackPanel>
</Window>
